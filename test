    function addTwoNumberRange100NoRem()
    {
        while(true)
        {
            var a=Math.floor(Math.random() * 10);
            var b=Math.floor(Math.random() * 10);
            var c=Math.floor(Math.random() * 10);
            var d=Math.floor(Math.random() * 10);
            var rs="";
            if(a+c<=9 && b+d<=9)
            {
                document.getElementById("trueResult").value =(a * 10 + b) + (c * 10 + d);
                return (a * 10 + b) + " + " + (c * 10 + d) + " = ";
            }
            
        }
    }
    function subTwoNumberRange100NoRem()
    {
        while(true)
        {
            var a=Math.floor(Math.random() * 10);
            var b=Math.floor(Math.random() * 10);
            var c=Math.floor(Math.random() * 10);
            var d=Math.floor(Math.random() * 10);
            var rs="";
            if(a-c>=0 && b-d>=0)
            {
                document.getElementById("trueResult").value =(a * 10 + b) - (c * 10 + d);
                return (a * 10 + b) + " - " + (c * 10 + d) + " = ";
            }
            
        }
    }
